<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_margin="8dp">

    <ImageView
        android:id="@+id/iv_item_of_list"
        android:layout_width="100dp"
        android:layout_height="90dp"
        android:background="#40D2DBDF"
        android:adjustViewBounds="true"
        app:layout_constraintEnd_toStartOf="@id/tv_item_of_list"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <TextView
        android:id="@+id/tv_item_of_list"
        android:layout_width="0dp"
        android:layout_height="90dp"
        android:background="#40D2DBDF"
        android:gravity="center"
        android:maxLines="3"
        android:textSize="@dimen/text_size_rec_item"
        android:textStyle="italic|bold"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toEndOf="@id/iv_item_of_list"
        app:layout_constraintTop_toTopOf="parent"
        tools:text="some text\n some text\n some text" />

</androidx.constraintlayout.widget.ConstraintLayout>